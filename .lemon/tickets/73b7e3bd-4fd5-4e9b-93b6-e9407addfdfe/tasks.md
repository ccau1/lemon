- [x] Implement server dynamic port fallback
  In `packages/server/src/index.ts`, change the `startServer` function signature so the `port` parameter defaults to `3456` internally when not provided. Add a `findAvailablePort(startPort: number, maxAttempts = 100)` helper that attempts `fastify.listen()` sequentially starting at `startPort`. On error code `EADDRINUSE`, increment the port by 1 and retry; on any other error, throw immediately. If no port is found after `maxAttempts`, throw an error. Log the final resolved listening port to the console. Modify `startServer` to return the resolved port number as a `number` so callers (Electron, CLI) can consume it. Ensure the function still accepts an optional explicit port override.
- [x] Update CLI serve command default port
  In `packages/cli/src/commands/serve.ts`, change the default port value from `3000` to `3456` in the command option or argument handling. Pass the resolved port into `startServer()` when invoking it. After `startServer` returns, log the actual listening port that the server resolved (especially important if the fallback mechanism selected a different port).
- [x] Update CLI entry point port options
  In `packages/cli/src/index.ts`, update the `--port` CLI option default string from `"3000"` to `"3456"`. Update the `getClient()` function so the default base URL uses `localhost:${process.env.LEMON_PORT || 3456}` instead of `localhost:${process.env.LEMON_PORT || 3000}`. Ensure the `LEMON_PORT` environment variable still overrides the default when present.
- [x] Update CLI ApiClient default base URL
  In `packages/cli/src/api-client.ts`, update the default base URL construction to use port `3456` instead of `3000` when no `LEMON_PORT` environment variable is provided. Ensure the constructor or factory still accepts an explicit base URL override and respects the `LEMON_PORT` env var.
- [x] Update Electron main for dynamic port
  In `packages/electron/src/main.ts`, remove any hardcoded `port: 3000` argument passed to `startServer()`. Call `startServer({ dataDir })` (or with an explicit `port: 3456` if desired) and `await` or `.then()` the returned Promise to capture the resolved port number. Store the resolved port in a local variable. Use this stored port when constructing the production file server URL and for any health-check polling loops, instead of assuming `3000`.
- [x] Add CLI cross-platform bundle script
  In `packages/cli/package.json`, add a `bundle:exe` script that: (1) runs `pnpm build` for the workspace dependency graph (`@lemon/shared` → `@lemon/server` → `@lemon/cli`) using `pnpm --filter @lemon/cli... run build`, (2) bundles `dist/index.js` and all `node_modules` dependencies into a single self-contained JavaScript entry using `esbuild` (or `rollup`), (3) compiles that entry into a native executable using `pkg` (or Node.js SEA) targeting the current platform, (4) explicitly includes `better-sqlite3` native `.node` binaries as bundled assets so the runtime can resolve them via `__dirname` / `import.meta.url` patched to `process.execPath`, and (5) outputs to `dist/lemon-cli` on macOS/Linux and `dist/lemon-cli.exe` on Windows.
- [x] Create GitHub Actions release workflow
  Create `.github/workflows/release-cli.yml` with three jobs. **Trigger:** `on.push.branches: [main]` with `paths:` filters for `packages/cli/**`, `packages/server/**`, `packages/shared/**`. **Version job:** checkout with `fetch-depth: 0`, find the latest `cli-*` tag via `git tag -l 'cli-*' --sort=-v:refname`, parse the semver suffix, bump the patch version (e.g., `cli-0.1.0` → `cli-0.1.1`), and export `NEW_TAG` and `VERSION` as job outputs. **Build job:** matrix across `macos-latest` (builds x64 and arm64 via `pkg --targets`) and `windows-latest` (builds x64); each runner checks out, sets up Node 20 + pnpm, runs `pnpm install`, runs `pnpm --filter @lemon/cli... build`, runs the bundle script, renames the binary to `lemon-cli-{VERSION}` (macOS) or `lemon-cli-{VERSION}.exe` (Windows), and uploads as a workflow artifact. **Release job:** depends on Version and Build; downloads all artifacts, creates and pushes the `cli-{VERSION}` git tag using `GITHUB_TOKEN` with write permissions, creates a GitHub Release for that tag using `gh release create` or `softprops/action-gh-release`, and attaches the three binaries.
- [x] Update REST API docs port reference
  In `docs/api/rest-api.md`, replace every occurrence of `http://localhost:3000` with `http://localhost:3456` in the Base URL section and any code examples. Ensure the document still accurately describes the endpoint paths and methods.
- [x] Update WebSocket API docs port reference
  In `docs/api/websocket.md`, replace `ws://localhost:3000/ws` with `ws://localhost:3456/ws` in the connection string. Ensure the event descriptions and payload examples remain unchanged.
- [x] Update CLI architecture docs port references
  In `docs/architecture/cli.md`, replace all `3000` references with `3456` in the `ApiClient` code snippet, the `serve` command example (`lemon serve -p 3000`), and any environment variable descriptions. Ensure the command descriptions and default workspace resolution section remain accurate.
- [x] Update Electron architecture docs port references
  In `docs/architecture/electron-app.md`, replace "port 3000" with "port 3456 (or dynamically resolved port)". Add a sentence describing that `startServer()` returns the actual listening port and the main process should use that returned value instead of hardcoding a port. Ensure the development server reference (`localhost:5173`) and native API descriptions remain unchanged.